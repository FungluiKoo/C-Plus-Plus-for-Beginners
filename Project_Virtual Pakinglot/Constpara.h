#include <windows.h>
#include <freeglut.h>

#ifndef _CP_H_
#define _CP_H_
#define pi 3.14159265
#define MS 60 //indicating the max drawing scale
#define MD 400 //indicating the max angle divide
#define ZERO 1e-5
#define FOR(i,l,r) for(int i=l;i<=r;++i)
#define FOD(i,r,l) for(int i=r;i>=l;--i)

#endif